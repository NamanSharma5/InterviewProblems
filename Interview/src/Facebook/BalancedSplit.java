package Facebook;

public class BalancedSplit {

  boolean balancedSplitExists(int[] arr) {
    /*
    Idea is to sort array - O(n log n) w/ merge sort
    -> sum array (if sum == odd then no solution)
    -> count from left of array and see if running total ever equals half of array sum
     */


    return true;
  }
}
